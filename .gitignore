node_modules/
dist/

# Model weights are NEVER committed. The CI guard and the pre-commit hook both
# fail if any of these appear, or if any file exceeds 10 MB.
*.gguf
*.onnx
*.safetensors
*.bin
*.pt
*.pth
models/

# macOS Finder metadata — never useful in a repository.
.DS_Store

# Release artefacts, produced by scripts/bundle-release.mjs.
build/
